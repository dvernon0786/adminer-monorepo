{
  "name": "adminer",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -w @adminer/web build && npm run -w @adminer/api build",
    "dev:api": "npm run -w @adminer/api dev",
    "dev:web": "npm run -w @adminer/web dev",
    "guard": "node scripts/guard.js",
    "scan:hardcoded": "tsx tools/hardcoded-scan.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "fast-glob": "^3.3.2",
    "tsx": "^4.19.1",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
