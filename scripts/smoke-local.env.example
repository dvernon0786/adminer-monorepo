# Local smoke test environment variables
# Copy this file to scripts/smoke-local.env and fill in your values

# Production domain
DOMAIN=https://www.adminer.online

# Clerk session JWTs for org-scoped users
# - CLERK_JWT_FREE should resolve to an org on Free plan
# - CLERK_JWT_PRO  should resolve to an org on Pro plan  
# - CLERK_JWT_ENT  should resolve to an org on Enterprise plan
CLERK_JWT_FREE=
CLERK_JWT_PRO=
CLERK_JWT_ENT=

# Optional explicit org IDs (only used for echo/logging)
ORG_ID_FREE=
ORG_ID_PRO=
ORG_ID_ENT=

# Usage:
# 1. Copy this file: cp scripts/smoke-local.env.example scripts/smoke-local.env
# 2. Fill in your Clerk JWT tokens
# 3. Run: source scripts/smoke-local.env && scripts/smoke.sh 